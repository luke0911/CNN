# 🎯 YOLO 모델 업데이트 로그

## 📅 2025-11-23 업데이트

### 🔄 변경 사항

**이전 모델 → 새 모델:**
- 위치: `app/src/main/assets/object.onnx`
- 모델: YOLOv8n → **YOLOv8s** (더 큰 모델)
- 데이터셋: 통합_데이터셋_v2 (38 epoch 학습 완료)

### 📊 성능 개선

| 클래스 | 이전 성능 | 새 모델 성능 | 개선율 |
|--------|----------|-------------|--------|
| **stairs (계단)** | 57.0% | **84.5%** | **+48%** ⭐ |
| exit_sign (비상구) | - | **99.5%** | 우수 |
| restroom_sign (화장실) | - | **99.3%** | 우수 |
| fire_extinguisher (소화기) | - | **95.6%** | 우수 |
| water_dispenser (정수기) | - | **90.6%** | 우수 |
| door (문) | - | **82.9%** | 양호 |
| elevator (엘리베이터) | - | 42.6% | 개선 필요 |
| stair_sign (계단 표지판) | - | 1.6% | 개선 필요 |

**전체 mAP50:** 74.6% (양호)

### 🎯 주요 특징

**계단 인식 대폭 개선:**
- mAP50: 57.0% → **84.5%** (+48%)
- Recall: 57.5% → **68.6%** (+19%)
- Precision: **90.8%** (매우 높음)

**모델 크기:**
- 파일 크기: 43MB (동일, ONNX 압축)
- 파라미터: 11.1M (YOLOv8n: 3.2M → YOLOv8s: 11.1M)

### 📱 앱 적용 방법

**1. 앱 빌드 (Android Studio)**
```bash
cd /Users/idohun/WorkSpace/CNN/App
./gradlew assembleDebug
```

**2. APK 설치**
```bash
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

**3. 테스트**
- 앱 실행
- OBJECT 모드 선택
- 계단 인식 테스트 (이전보다 훨씬 정확함)

### 🔧 기술 세부사항

**변환 정보:**
- 원본: `runs/train/stairs_yolov8s_30epoch_clean2/weights/best.pt`
- ONNX: `best.onnx` (~~opset 22~~ → **opset 21**, simplified)
- 입력 크기: 640x640
- 출력: (1, 12, 8400) - 8개 클래스
- **✅ opset 21로 재변환** (Android ONNX Runtime 호환성)

**클래스 매핑 (앱과 일치):**
```kotlin
0: door (문)
1: exit_sign (비상구 표지판)
2: fire_extinguisher (소화기)
3: elevator (엘리베이터)
4: restroom_sign (화장실 표지판)
5: water_dispenser (정수기)
6: stair_sign (계단 표지판)
7: stairs (계단)
```

### 💾 백업

**이전 모델 백업 위치:**
- `app/src/main/assets/object.onnx.backup`
- 날짜: 2025-09-05 16:50

**복원 방법 (필요시):**
```bash
cd /Users/idohun/WorkSpace/CNN/App/app/src/main/assets
mv object.onnx object.onnx.new
mv object.onnx.backup object.onnx
```

### ⚠️ 문제 해결: ONNX Opset 호환성

**문제:**
- 모델 로딩 실패: "Opset 22 is under development"
- Android ONNX Runtime은 opset 21까지만 공식 지원

**해결:**
1. **opset 21로 재변환** (onnx변환_opset21.py)
   ```bash
   cd /Users/idohun/WorkSpace/CNN/YOLO_Project/object_detection
   python3 onnx변환_opset21.py
   ```
2. 기존 opset 22 모델 백업: `object.onnx.opset22_backup`
3. 새 opset 21 모델 배포: 42.68 MB
4. 앱 재빌드 및 설치 완료

### 🔧 앱 코드 수정 (8클래스 지원)

**1. Constants.kt 수정**
- 추가: `YOLO_STANDARD_CHANNELS_12 = 12` (4 bbox + 8 classes)

**2. YoloOutputParser.kt 수정**
- `parseExplicit2D()`: 12채널 처리 추가
- `parseAs2DCxN()`: 12채널 체크 추가
- `parseGeneric()`: 12채널 처리 추가
- `findBestClassification()`: 8클래스 모델 우선 처리 (indices 4-11)
- `isChannelDimension()`: 12채널 인식 추가

**3. OverlayView.kt 수정**
- 객체 색상 표시 로직에 "계단" 클래스 추가

**4. 빌드 완료**
```bash
cd /Users/idohun/WorkSpace/CNN/App
./gradlew assembleDebug
# BUILD SUCCESSFUL
```

### 🚀 다음 단계

**v3 데이터셋 학습 진행 중:**
- elevator: 275개 → **550개** (+100%)
- stair_sign: 231개 → **462개** (+100%)
- 예상 완료: 약 2시간 후
- 예상 성능: elevator 70-80%, stair_sign 60-70%

**완료 후 업데이트 예정**

---

## 📝 참고

**학습 로그:**
- 위치: `/Users/idohun/WorkSpace/CNN/YOLO_Project/object_detection/runs/train/stairs_yolov8s_30epoch_clean2`
- Results: `results.csv`, `results.png`
- Config: `args.yaml`

**성능 평가 재실행:**
```bash
cd /Users/idohun/WorkSpace/CNN/YOLO_Project/object_detection
python3 -c "
from ultralytics import YOLO
model = YOLO('runs/train/stairs_yolov8s_30epoch_clean2/weights/best.pt')
results = model.val(data='통합_데이터셋_v2/data.yaml', split='test', verbose=True)
"
```
